# Session Summary - 2026-01-05

## 🎯 Goal
Set up staging/production deployment workflow for birthday-of-love wedding website on NAS.

## ✅ Completed Tasks

### 1. Repository Cleanup
- Deleted `feature/apartment-scraper` branch (no longer relevant)
- Removed associated git worktree

### 2. New Dedicated Repository
- Created dedicated GitHub repo: `https://github.com/tadan/birthday-of-love.git`
- Fixed repository structure (was pushing entire parent workspace, now standalone)
- Initialized git in birthday-of-love directory
- Force pushed clean structure to GitHub

### 3. Branch Setup
- Created `main` branch for production
- Created `staging` branch for testing
- Both branches pushed to GitHub

### 4. Deployment Script
- Updated `deploy.sh` to support staging/production environments
- Script handles:
  - Git operations (commit, push)
  - SSH deployment to NAS
  - Automated builds on NAS
  - Verification of deployment
- Fixed build directory path (uses `build/` not `dist/`)

### 5. NAS Deployment Setup
- Set up SSH authentication (existing key worked)
- Used Personal Access Token for HTTPS git clone
- Created two deployment directories:
  - `/volume1/docker/birthday-of-love-prod` (main branch)
  - `/volume1/docker/birthday-of-love-staging` (staging branch)
- Built both environments successfully
- Created symlinks:
  - Production: `/volume1/docker/begga.daniele.is` → `birthday-of-love-prod/build`
  - Staging: `/volume1/docker/staging.begga.daniele.is` → `birthday-of-love-staging/build`

### 6. Production Switch
- Backed up original deployment to `begga.daniele.is.backup`
- Atomically switched to git-managed deployment
- **Production site verified working**: https://begga.daniele.is

## 📝 Key Decisions

1. **Dedicated Repo**: Separated birthday-of-love from claude-workspace for cleaner structure
2. **Build Directory**: Uses Vite with `outDir: 'build'` (not `dist`)
3. **Authentication**: Used Personal Access Token for git operations (SSH key was deploy key for different repo)
4. **Zero Downtime**: Atomic symlink switch with instant rollback capability

## 📁 Files Modified

### Created
- `SESSION-2026-01-05.md` (this file)

### Updated
- `deploy.sh` - Added staging/production support, fixed build paths
- `DEPLOYMENT.md` - Updated with git-based workflow and NAS setup instructions

## 🚀 Deployment Workflow

### For Future Updates:

```bash
# 1. Make changes locally
cd /Users/danieletatasciore/Documents/repos/claude/birthday-of-love
# Edit files...

# 2. Test locally
npm run dev

# 3. Deploy to staging
./deploy.sh staging

# 4. Test at https://staging.begga.daniele.is (once nginx configured)

# 5. Deploy to production
./deploy.sh production
```

## ⚠️ NEXT STEP (CRITICAL)

### Configure Nginx for Staging Subdomain

**Status**: Staging build is ready but nginx not configured yet

**What to do**:
1. SSH to NAS: `ssh daniele@192.168.1.115`
2. Check existing nginx config:
   ```bash
   cat /usr/local/etc/nginx/sites-enabled/server.webstation-vhost.conf | grep -A 10 "begga.daniele.is"
   ```
3. Add staging subdomain configuration to serve `staging.begga.daniele.is` from `/volume1/docker/staging.begga.daniele.is`
4. Test nginx config: `sudo nginx -t`
5. Reload nginx: `sudo nginx -s reload`
6. Update Cloudflare DNS if needed (CNAME for staging subdomain)

**Current State**:
- ✅ Production: https://begga.daniele.is (working)
- ⏳ Staging: https://staging.begga.daniele.is (build ready, nginx not configured)

## 🔧 Technical Details

### NAS Paths
```
/volume1/docker/
├── begga.daniele.is -> birthday-of-love-prod/build (symlink)
├── staging.begga.daniele.is -> birthday-of-love-staging/build (symlink)
├── begga.daniele.is.backup (backup of original)
├── birthday-of-love-prod/ (git repo - main branch)
│   └── build/ (vite build output)
└── birthday-of-love-staging/ (git repo - staging branch)
    └── build/ (vite build output)
```

### Build Configuration
- Build tool: Vite 6.3.5
- Output directory: `build/`
- Entry point: `index.html`
- Dev server: Port 3000

### Git Configuration
- Remote: https://github.com/tadan/birthday-of-love.git
- Production branch: `main`
- Staging branch: `staging`

## 📊 Session Stats
- Files modified: 2
- Commits created: 3
- Git branches: 2 (main, staging)
- Deployments: 2 (production, staging)
- Downtime: ~2 seconds (atomic switch)

---

## 🎬 Resume Point

**Start next session with**: Configuring nginx for staging subdomain (see NEXT STEP section above)
