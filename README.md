
  # Wedding Save the Date

  This is a code bundle for Wedding Save the Date. The original project is available at https://www.figma.com/design/6SMiTGpfxFdx9U9fDHPysm/Wedding-Save-the-Date.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  